#Write an if statement that assigns 0 to the varable b, and assigns 1 to the
#variable c, if the variable a is less than 10.

import random
a = random.randint(0, 20)

if a < 10:
    b = 0
    c = 1
    print(b, c)
else:
    print()

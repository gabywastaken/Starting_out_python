#Write an if-else statement that displays "speed is normal" if the speed vaiable
#is within 24 to  56. If the speed variabl's value is outside this  range, display
#"speed is abnormal"

speed_value = int(input("Add speed value: "))
 
if speed_value > 24 and speed_value < 56:
    print("Speed is normal")
else:
    print("Speed is abnormal")
